{
  "name": "staia-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && eleventy",
    "dev": "eleventy --serve",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}
